import { ClassValue } from 'clsx';
import { ForwardRefExoticComponent } from 'react';
import { JSX } from 'react/jsx-runtime';
import { RefAttributes } from 'react';

/**
 * Converts a Gregorian (AD) date to a Bikram Sambat (BS) date.
 *
 * @param year - AD Year
 * @param month - AD Month
 * @param day - AD Day
 * @returns NepaliDate object containing BS year, month, and day
 * @throws Error if the AD date is invalid or outside supported range
 */
export declare function adToBs(year: number, month: number, day: number): NepaliDate;

/**
 * Converts an AD date to a BS date string.
 *
 * @param year - AD Year
 * @param month - AD Month
 * @param day - AD Day
 * @param format - Format for BS date output
 * @param language - Language for output
 * @returns Formatted BS date string
 */
export declare function adToBsString(year: number, month: number, day: number, format?: string, language?: Language): string;

export declare const BS_MAX_YEAR = 2100;

export declare const BS_MIN_YEAR = 2000;

/**
 * Converts a Bikram Sambat (BS) date to a Gregorian (AD) date.
 *
 * @param year - BS Year
 * @param month - BS Month
 * @param day - BS Day
 * @returns EnglishDate object containing AD year, month, and day
 * @throws Error if the BS date is invalid
 */
export declare function bsToAd(year: number, month: number, day: number): EnglishDate;

/**
 * Converts a BS date to an AD date string.
 *
 * @param year - BS Year
 * @param month - BS Month
 * @param day - BS Day
 * @param format - Format for AD date output
 * @returns Formatted AD date string
 */
export declare function bsToAdString(year: number, month: number, day: number, format?: string): string;

/**
 * A highly customizable Nepali (Bikram Sambat) Calendar component.
 * Supports both single date and range selection, localized display, and custom constraints.
 */
export declare function Calendar({ mode, selected, onSelect, selectedRange, onRangeSelect, minDate, maxDate, language, defaultMonth, className, captionLayout, }: CalendarProps): JSX.Element;

/**
 * Props for the Calendar component.
 */
export declare interface CalendarProps {
    /**
     * Selection mode. 'single' for selecting one date, 'range' for a start and end date.
     * @default 'single'
     */
    mode?: 'single' | 'range';
    /** Selected date for single mode */
    selected?: NepaliDate;
    /** Callback when a date is selected in single mode */
    onSelect?: (date: NepaliDate) => void;
    /** Selected range for range mode */
    selectedRange?: DateRange;
    /** Callback when a range is selected in range mode */
    onRangeSelect?: (range: DateRange) => void;
    /** Minimum selectable date */
    minDate?: NepaliDate;
    /** Maximum selectable date */
    maxDate?: NepaliDate;
    /**
     * Display language for month names, day names, and numerals.
     * @default 'en'
     */
    language?: Language;
    /** The month initially displayed in the calendar */
    defaultMonth?: NepaliDate;
    /** Additional CSS classes for the container */
    className?: string;
    /**
     * Strategy for displaying the month/year selection.
     * @default 'buttons'
     */
    captionLayout?: 'buttons' | 'dropdown';
}

/**
 * Utility function to merge Tailwind CSS classes safely using clsx and tailwind-merge.
 *
 * @param inputs - Array of class names or conditional class objects
 * @returns Optimized class string
 */
export declare function cn(...inputs: ClassValue[]): string;

/**
 * Compares two Bikram Sambat dates.
 *
 * @param a - First date
 * @param b - Second date
 * @returns -1 if a < b, 0 if a === b, 1 if a > b
 */
export declare function compareBsDates(a: NepaliDate, b: NepaliDate): number;

/**
 * Represents a range of Nepali dates.
 */
export declare type DateRange = {
    /** The start of the range */
    from?: NepaliDate;
    /** The end of the range */
    to?: NepaliDate;
};

/**
 * A modern Nepali Date Range Picker component with native form and React Hook Form support.
 */
export declare const DateRangePicker: ForwardRefExoticComponent<DateRangePickerProps & RefAttributes<HTMLButtonElement>>;

/**
 * Props for the DateRangePicker component.
 */
export declare interface DateRangePickerProps extends Omit<CalendarProps, 'selected' | 'onSelect' | 'selectedRange' | 'onRangeSelect' | 'mode'> {
    /** The currently selected date range { from, to }. */
    value?: DateRange;
    /** Callback triggered when the range changes. */
    onChange?: (range: DateRange | undefined) => void;
    /** Callback triggered when the component loses focus. */
    onBlur?: () => void;
    /** Name for the 'from' date field. */
    nameFrom?: string;
    /** Name for the 'to' date field. */
    nameTo?: string;
    /** Placeholder text. */
    placeholder?: string;
    /** Date display format. */
    dateFormat?: string;
    /** Whether disabled. */
    disabled?: boolean;
    /** Extra CSS classes. */
    className?: string;
    /** Extra CSS classes for the calendar. */
    calendarClassName?: string;
    /** Close popover on range completion. */
    closeOnSelect?: boolean;
}

/**
 * Represents a date in the Gregorian (AD) calendar system.
 */
export declare interface EnglishDate {
    /** The year in Gregorian (e.g., 2026) */
    year: number;
    /** The month (1-indexed, 1-12) */
    month: number;
    /** The day of the month (1-indexed, up to 31) */
    day: number;
}

/**
 * Formats a Gregorian (AD) date object using a pattern.
 *
 * @param date - EnglishDate object { year, month, day }
 * @param format - Pattern string
 * @returns Formatted AD string
 */
export declare function formatAdDate(date: EnglishDate, format?: string): string;

/**
 * Formats a Bikram Sambat (BS) date based on a pattern.
 *
 * Supported tokens:
 * - `YYYY`: Full year (e.g., 2081 or २०८१)
 * - `YY`: Two-digit year (e.g., 81 or ८१)
 * - `MMMM`: Full month name (e.g., Baisakh or बैशाख)
 * - `MMM`: Short month name (e.g., Bai or बैशा)
 * - `MM`: Two-digit month number (e.g., 01 or ०१)
 * - `M`: Month number (e.g., 1 or १)
 * - `DD`: Two-digit day number (e.g., 01 or ०१)
 * - `D`: Day number (e.g., 1 or १)
 *
 * @example
 * formatBsDate({year: 2082, month: 10, day: 6}, 'MMMM D, YYYY', 'ne') // "माघ ६, २०८२"
 *
 * @param date - The NepaliDate object to format
 * @param format - The format pattern string (default: 'YYYY-MM-DD')
 * @param language - Output language (default: 'en')
 * @returns Formatted date string
 */
export declare function formatBsDate(date: NepaliDate, format?: string, language?: Language): string;

/**
 * Convenience function to format a NepaliDate object.
 * Alias for formatBsDate.
 *
 * @param date - The NepaliDate object
 * @param format - Format pattern
 * @param language - Output language
 * @returns Formatted string
 */
export declare function getBsDateString(date: NepaliDate, format?: string, language?: Language): string;

/**
 * Gets the localized short name of a day of the week.
 *
 * @param dayOfWeek - Day index (0=Sunday, 6=Saturday)
 * @param language - Language ('ne' or 'en')
 * @returns Localized day name
 */
export declare function getDayName(dayOfWeek: number, language?: Language): string;

/**
 * Returns an array of all localized short day names for a given language.
 *
 * @param language - Language ('ne' or 'en')
 * @returns Array of 7 day names
 */
export declare function getDayNames(language?: Language): string[];

/**
 * Gets the number of days in a specific month of a Bikram Sambat year.
 *
 * @param year - The BS year
 * @param month - The BS month (1-12)
 * @returns Number of days in the specified month
 * @throws Error if the year or month is invalid
 */
export declare function getDaysInBsMonth(year: number, month: number): number;

/**
 * Determines the day of the week (0-6) for the first day of a BS month.
 * 0 is Sunday, 6 is Saturday.
 *
 * @param year - The BS year
 * @param month - The BS month
 * @returns The weekday index (0-6)
 */
export declare function getFirstDayOfBsMonth(year: number, month: number): number;

/**
 * Gets the localized name of a Bikram Sambat month.
 *
 * @param month - Month number (1-12)
 * @param language - Language ('ne' or 'en')
 * @returns Month name
 */
export declare function getMonthName(month: number, language?: Language): string;

/**
 * Returns an array of allLocalized month names for a given language.
 *
 * @param language - Language ('ne' or 'en')
 * @returns Array of 12 month names
 */
export declare function getMonthNames(language?: Language): string[];

/**
 * Gets today's date in Bikram Sambat calendar.
 *
 * @returns NepaliDate object representing today
 */
export declare function getTodayBs(): NepaliDate;

/**
 * Gets today's BS date as a formatted string.
 *
 * @param format - Output format pattern
 * @param language - Output language
 * @returns Formatted today's date string
 */
export declare function getTodayBsString(format?: string, language?: Language): string;

/**
 * Check if a Bikram Sambat date is within a specific range (inclusive).
 *
 * @param date - The date to check
 * @param minDate - Minimum date of the range
 * @param maxDate - Maximum date of the range
 * @returns True if date is within [minDate, maxDate]
 */
export declare function isBsDateInRange(date: NepaliDate, minDate?: NepaliDate, maxDate?: NepaliDate): boolean;

/**
 * Validates if the given Bikram Sambat date is a real date.
 *
 * @param year - The BS year
 * @param month - The BS month
 * @param day - The BS day
 * @returns True if the date exists in the BS calendar, false otherwise
 */
export declare function isValidBsDate(year: number, month: number, day: number): boolean;

/** Supported languages for date display */
export declare type Language = 'ne' | 'en';

export declare const NEPALI_DAYS: {
    ne: string[];
    en: string[];
};

export declare const NEPALI_DAYS_FULL: {
    ne: string[];
    en: string[];
};

export declare const NEPALI_MONTHS: {
    ne: string[];
    en: string[];
};

export declare const NEPALI_NUMERALS: string[];

/**
 * Represents a date in the Bikram Sambat (BS) calendar system.
 */
export declare interface NepaliDate {
    /** The year in Bikram Sambat (e.g., 2082) */
    year: number;
    /** The month (1-indexed, 1-12) */
    month: number;
    /** The day of the month (1-indexed, up to 32) */
    day: number;
}

/**
 * Parses a date string in `YYYY-MM-DD` format into a NepaliDate object.
 *
 * @param dateString - String to parse (e.g., "2082-10-06")
 * @returns NepaliDate object or null if parsing fails
 */
export declare function parseBsDate(dateString: string): NepaliDate | null;

/**
 * A modern Nepali Date Picker component with native form and React Hook Form support.
 */
export declare const Picker: ForwardRefExoticComponent<PickerProps & RefAttributes<HTMLButtonElement>>;

/**
 * Props for the Picker component.
 */
export declare interface PickerProps extends Omit<CalendarProps, 'selected' | 'onSelect'> {
    /** The currently selected Nepali date. */
    value?: NepaliDate;
    /** Callback triggered when a new date is selected. */
    onChange?: (date: NepaliDate | undefined) => void;
    /** Callback triggered when the component loses focus. */
    onBlur?: () => void;
    /** Name of the field for form integration. */
    name?: string;
    /** Unique ID for the component. */
    id?: string;
    /** Placeholder text shown when no date is selected. */
    placeholder?: string;
    /** The format pattern used to display the date in the trigger. */
    dateFormat?: string;
    /** Whether the date picker is disabled. */
    disabled?: boolean;
    /** Additional CSS classes for the trigger button. */
    className?: string;
    /** Additional CSS classes for the calendar component inside popover. */
    calendarClassName?: string;
    /** Whether to automatically close the popover after selection. */
    closeOnSelect?: boolean;
}

/**
 * A variant of the Picker that includes a "Today" shortcut button.
 */
export declare function PickerWithToday({ containerClassName, todayButtonClassName, ...props }: PickerWithTodayProps): JSX.Element;

/**
 * Props for the PickerWithToday component.
 */
export declare interface PickerWithTodayProps extends PickerProps {
    /** Additional CSS classes for the container div. */
    containerClassName?: string;
    /** Additional CSS classes for the "Today" button. */
    todayButtonClassName?: string;
}

/**
 * Converts standard Arabic numerals to Nepali numerals.
 *
 * @example
 * toNepaliNumeral(2082) // Returns "२०८२"
 *
 * @param num - The number to convert
 * @returns A string containing the equivalent Nepali numerals
 */
export declare function toNepaliNumeral(num: number): string;

export { }
